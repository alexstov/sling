package util_test

import (
	. "github.com/onsi/ginkgo"
	// . "github.com/onsi/gomega"
)

var _ = Describe("StringUtil", func() {
	BeforeEach(func() {
	})

	Describe("Emul MultiSend called", func() {
		Context("with args", func() {
			It("delayMs applied, requests pop from the reqQueue on damand, not all at once.", func() {
			})
		})
	})
})
